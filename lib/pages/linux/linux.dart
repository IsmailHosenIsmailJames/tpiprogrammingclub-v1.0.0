import 'package:flutter/material.dart';

class LinuxSystem extends StatefulWidget {
  const LinuxSystem({super.key});

  @override
  State<LinuxSystem> createState() => _LinuxSystemState();
}

class _LinuxSystemState extends State<LinuxSystem> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Under Development'),
      ),
    );
  }
}
